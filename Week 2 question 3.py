from math import *
def HPS (N):
    A = sqrt(N) #Square root of N (input number)
    B = int(A) #Turning it to an integer becuase it could be a float because
               #it won't always be a whole number and could be a decimal
    C = B**2 #Performs B to the power of 2 to get the closest whole number
             #and square number
    print ((str('')) + (str(C)))
HPS(20)    










